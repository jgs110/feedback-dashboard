# Data Mode Configuration
# Set to "mock" to use in-memory mock data
# Set to "live" to connect to the Cloudflare Worker API
VITE_DATA_MODE=mock

# API Base URL (optional)
# Leave empty for same-origin requests in production
# Set to http://localhost:8787 for local Wrangler dev server
# VITE_API_BASE_URL=http://localhost:8787
